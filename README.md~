rscr
====

Las dependencias están definidas en el fichero Package.json

Para instalarlas basta con ejecutar 'sudo npm install'

Para ejecutar el servidor hay que ejecutar 'npm start' o 'node app.js' 
